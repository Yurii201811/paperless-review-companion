# Roadmap

- Configurable taxonomy files.
- Richer language-specific rules.
- HTML report export.
- Batch resume command that skips already-reviewed document IDs.
- Optional document-type write-back once Paperless API behavior is proven across
  versions.
- Built-in backup reminder/check before first live write-back.
- More sample fixtures and golden report snapshots.

